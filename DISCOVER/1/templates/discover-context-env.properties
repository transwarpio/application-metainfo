<#--handle dependent.zookeeper-->
<#if dependencies.ZOOKEEPER??>
<#assign zookeeper=dependencies.ZOOKEEPER quorums=[]>
<#list zookeeper.roles.ZOOKEEPER as role>
<#assign quorums += [role.hostname]>
</#list>
<#assign quorum = quorums?join(",")>
</#if>
DISCOVER_EXECUTOR_MEMORY=${service['discover.executor.memory']}M
DISCOVER_EXECUTOR_CORES=${service['discover.executor.cores']}
DISCOVER_NUMBER_EXECUTORS=${service['discover.number.executors']}
DISCOVER_YARN_APP_NAME=${service.sid}-discover
DISCOVER_YARN_QUEUE=${service['discover.yarn.queue']}
DISCOVER_UI_PORT=${service['discover.ui.port']}
LICENSE_ZK_QUORUM=${quorum}
DISCOVER_NAME=${service.sid}
DISCOVER_USER_HOME=/tmp
