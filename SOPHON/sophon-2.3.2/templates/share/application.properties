server.address = ${service.roles.SHARE_SERVER[0].hostname}
server.port = 8068

<#if dependencies.TXSQL??>
<#assign mysqlHostPorts = []/>
    <#list dependencies.TXSQL.roles['TXSQL_SERVER'] as role>
        <#assign mysqlHostPorts = mysqlHostPorts + [role.hostname + ':' + dependencies.TXSQL['mysql.rw.port']]>
    </#list>
<#assign mysqlHostPort = mysqlHostPorts?join(",")>
spring.datasource.url=jdbc:mysql://${mysqlHostPort}/metastore_share_${service.sid}?useUnicode=yes&characterEncoding=UTF-8&autoReconnect=true&autoReconnectForPools=true&failOverReadOnly=false&connectTimeout=10000&retriesAllDown=0&secondsBeforeRetryMaster=0&queriesBeforeRetryMaster=0
spring.datasource.username=${service['livy.jdbc.username']}
spring.datasource.password=${service['livy.jdbc.password']}
</#if>


#spring.datasource.url=jdbc:h2:mem:sophon_db;DATABASE_TO_UPPER=false;MODE=MYSQL;\
#DB_CLOSE_ON_EXIT=FALSE;
#spring.h2.console.enabled=true


liquibase.enabled=true
spring.jpa.hibernate.ddl-auto=none

liquibase.change-log=classpath:/db/master.changelog.xml
cas.service.prefix=https://${dependencies.GUARDIAN.roles.CAS_SERVER[0]['hostname']}:${dependencies.GUARDIAN['cas.server.ssl.port']}/cas

