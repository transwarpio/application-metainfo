spring.datasource:
  password: "${service['root.password']}"
manager-proxy.password: "${managerInfo['AQUILA_PASS']}"
auth:
  internal-admin:
    user: ${service['server.internal.admin.user']}
    pass: ${service['server.internal.admin.password']}